apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default # Change this to your target namespace if needed
spec:
  podSelector: {} # An empty selector matches ALL pods in the namespace
  policyTypes:
  - Ingress
  - Egress